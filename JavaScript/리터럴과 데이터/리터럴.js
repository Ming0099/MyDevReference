/* 리터럴이라는 단어는 값을 프로그램 안에서 직접 지정한다는 의미 */

let room1 = "conference_room_a"; // "conference_room_a" 따옴표 안은 리터럴

let currentRoom = room1; // currentRoom의 값은 room1의 값 "conference_room_a"과 같다.

 //currentRoom = conference_room_a;
 // 에러 발생 -> conference_room_a 이라는 식별자는 존재하지 않음.